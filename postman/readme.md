permission types and permission API only available, 

-> Create Permission type 
ex=> User

and create permission based on the permission type 

-> name = Listing User
-> slug = listing-user
-> type_id = permission_type_id

then the role permission can be added for admin